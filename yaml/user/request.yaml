components:
  schemas:
    userId:
      - in: path
        name: userId
        description: ユーザーID
        example: 1
        required: true
        schema:
          type: integer
          enum: [1,2,3,4]
      - in: query
        name: userId2
        description: ユーザーID
        example: 1
        required: true
        schema:
          type: integer
          enum: [1,2,3,4]

